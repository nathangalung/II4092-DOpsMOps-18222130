# End-to-End Verification Status — 2026-06-28

Single-node k3s, spinning disk. Deliverable per advisor framing: **every pipeline link
verified carrying current real data**, NOT zero-red-pods (zero-red is hardware-bounded
on this node — the durable fix is SSD or KEDA scale-to-zero, not software).

## PRIMARY crypto E2E — VERIFIED REAL + CURRENT

| Link | Evidence | Status |
|------|----------|--------|
| Ingestion — Coinbase OHLCV | `bronze.crypto_ohlcv` 513,603 rows, latest 15:43 (≈now) | ✅ live |
| Ingestion — supplementary | `bronze.crypto_sentiment` 38,380 rows, real fear-greed (scores 8–50) | ✅ live |
| Processing — stream (Flink) | `crypto-stream-processor` RUNNING; `bronze.crypto_ohlcv_features` 222→2934, latest 15:41 | ✅ live |
| Processing — vector | `crypto-vector-embedding` CronJob 3× Complete (every 5m) | ✅ live |
| Processing — batch (Airflow) | **RESTORED this session** — see below; `gold.fct_training_data` refreshed 10:51→**15:26** | ✅ proven |
| DWH — medallion | bronze/silver/gold schemas; `gold.fct_training_data` 513,569 rows | ✅ |
| Drift | `gold.drift_multi_scale` 28 rows; comparable adjacent equal-length windows (month/day) | ✅ |
| Train / Serve | MLflow run r2≈0.96 (persistence-inflated — flag in report); crypto-predictor tracks regimes | ✅ (prior session) |
| GitOps (use-case) | **REACTIVATED this session** — use-case-crypto.git seeded + binding Argo app | ✅ |

## KEYSTONE FIX THIS SESSION — batch orchestration was silently down

**Root cause:** `gitea/platform/use-case-crypto.git` was empty (never seeded) → platform
Airflow git-synced `platform.git` (no DAGs) → **Airflow had 0 DAGs**. Batch/lakehouse
orchestration was not running; gold had last refreshed ~4h earlier by a one-off.

**Fix (reproducible):**
1. `make seed-gitea` → pushed platform (966 files) + use-case-crypto (274 files incl `dags/`)
2. `kubectl apply -f use-case-crypto/manifests/airflow-dags-binding/dags-repo-override.yaml -n data-processing`
   + `kubectl apply -f use-case-crypto/argocd/airflow-dags-binding.yaml`
3. `kubectl rollout restart deploy/airflow-scheduler deploy/airflow-dag-processor -n data-processing`

**Verified:** 5 DAGs load (`crypto_lakehouse`, `crypto_hourly_features`,
`crypto_data_quality_gate` active; `crypto_daily_backfill` paused). A scheduled run
executed → `gold.fct_training_data` refreshed 10:51→15:26. KPO `delete_pod` fix (agent)
now live → no more orphan Error pods.

## SECONDARY gaps — IO-bound; root-cause + exact source fix (one-command when headroom exists)

| Gap | Root cause | Source fix | Re-run |
|-----|-----------|-----------|--------|
| iris canary (platform self-test) | `train-iris-model` init (FLAML+sklearn, ~6min disk-bound) cannot complete under PSI-io >50%; restart-loops | `platform/components/storage/minio/bucket-bootstrap.yaml` — reduce train cost or gate on quiet cluster | Argo re-syncs platform-storage |
| kafka-connect down | Strimzi KafkaConnect **build failed** (bakes Iceberg+Debezium plugins) — `kafka-connect-connect-build` stuck/failed under IO load | `platform/components/data-ingestion/kafka-connect/` build config | re-apply data-ingestion |
| lakekeeper warehouse (0) | iceberg-sink Job init waits on kafka-connect `/connectors` (down) → can't register; warehouse step needs lakekeeper only (healthy 43h) | `use-case-crypto/manifests/base/connectors/iceberg-sink.yaml` (already has x-project-id fix) | re-run Job after kafka-connect up |
| feature-cache → valkey (online cache) | **STALE IMAGE**, not a manifest bug. `config.rs:24` correctly reads `FCACHE__` + `config.rs:32` injects redis password; manifest env now `FCACHE__REDIS_URL` (applied live). But running binary predates that config fix → still defaults to `localhost:6379` → `Connection refused`. **NON-BLOCKING**: online serving already works via Feast materialize → ml-bridge (#491/#492), independent of feature-cache. | `platform/services/serving/feature-cache/` (source OK) | `make usecase-crypto-build` (rebuild crypto-feature-cache) + rollout. Also confirm valkey password env key matches `read_redis_password_env`. |
| feature-cache securityContext | base deployment lacks restricted-PSA `securityContext` (PSA warn, not enforce — pod runs) | `platform/services/base/deployments/feature-cache.yaml` add runAsNonRoot/drop-ALL/seccomp/allowPrivilegeEscalation=false | next apply |
| chaos-mesh / otel OutOfSync | agent's `ignoreDifferences` edits seeded, not yet Argo-synced (cosmetic) | helm-release.yaml (done) | Argo sync |

## SERVING-LAYER RECONCILIATION (this session)
- **ml-bridge RESTORED** ✅ — was absent (online read bridge). Root cause: its Argo **Rollout** referenced AnalysisTemplate `crypto-success-rate-p99` which was never applied → Rollout invalid/Degraded → no pod. Applied the AnalysisTemplate (extracted from overlay render) → Rollout Progressing → **pod Running**. Image `crypto-ml-bridge:latest` was in registry all along.
- **feature-cache scaled to 0** — redundant online writer (Feast materialize already writes valkey; ml-bridge reads valkey via Feast, NOT feature-cache's `features:<sym>` keys), gRPC :50053 has zero consumers, and image is stale anyway. Reversible. **OPEN: retire at source** (`config/services.yaml` feature_cache.enabled=false) vs keep — affects thesis narrative, so flagged not auto-applied.

## ROOT CAUSE of partial deployment + the reproducible full-fix
- ml-bridge Rollout AND its AnalysisTemplate were **present in source but missing live** → the use-case deployment is **partial** (manual `kubectl apply` left gaps).
- WHY: `use-case-crypto.git` was empty (the keystone bug), so the use-case **ApplicationSet was never deployable** → no Argo sync → only manual applies, which miss resources.
- **NOW UNBLOCKED:** repo is seeded. The reproducible completion = `make usecase-crypto-argocd` (applies `argocd/application.yaml` + `applicationset.yaml`) → Argo syncs the FULL use-case from Gitea → self-heals every partial-deploy gap (ml-bridge, AnalysisTemplate, anything else) declaratively. CAVEAT: if feature_cache stays enabled in the repo, Argo selfHeal will scale it back to 1 — disable in source first if retiring. This is one heavy reconcile op (gate on load).

## SINGLE-NODE IO CEILING (the real constraint)

Full pipeline (stream + hourly batch + platform) saturates the spinning disk: PSI-io >50%,
load steady ~25–30, spikes to 40–127 when hourly DAG runs (`hourly_features` :30,
`quality_gate` :45) stack with churn (Argo reconcile / iris loop / ad-hoc agents). MinIO
quorum held (no true cascade) but it's at the edge.

**Recommended single-node tuning** (matches "scale on load, don't run idle-heavy"):
- lakehouse already 6-hourly (`15 */6 * * *`). Consider relaxing `hourly_features`/
  `quality_gate` from hourly if steady-state proves unsustainable.
- Durable fix (advisor): SSD, or KEDA scale-to-zero for idle services.

## OPERATING NOTES
- Heavy ops MUST be serialized one-at-a-time, load-gated; concurrent IO bursts tip the node.
- Do not run ad-hoc cross-namespace audit agents concurrently with batch — they add CPU and
  helped tip load to 127 this session.

---

# POST-RESTART RECOVERY + SCALE-TO-ZERO — 2026-06-28 ~21:05 UTC

## Cluster recovery (cascade cleared)
- The GitOps-activation IO cascade (load 130–254, iowait 70%) was cleared by the user's
  **`sudo systemctl restart k3s`** (k3s PID started 20:27 UTC). PVCs + Gitea intact.
- ~35 min post-restart: load 36–55 (cold-start CPU backlog), **iowait oscillating 18–47%**
  (NOT the 70% cascade). Disk is the recovering constraint — new-pod init is slow
  (lakehouse KPO child stuck PodInitializing 9 min). Still settling; hold heavy ops.
- `make phase-full` deliberately **NOT run**: platform already deployed + reconciled (Argo
  apps Synced/Healthy); phase-full is the ~30-component IO burst that has the same shape as
  the op that cascaded the node. User delegated the call ("if need phase full… if not,
  straight to build+up") — took the out. This IS the best-proper-way on one spinning disk.

## Verified E2E freshness (now 21:04 UTC) — the deliverable is "every link carries CURRENT data"
| Stage | rows | latest | Δ | verdict |
|-------|------|--------|---|---------|
| bronze.crypto_ohlcv (Coinbase ingest) | 761,954 | 21:02 | ~2m | ✅ LIVE |
| bronze.crypto_ohlcv_features (Flink stream) | 439,339 | 21:00 | ~4m | ✅ LIVE |
| silver.stg_ohlcv | 514,072 | 21:02 | ~2m | ✅ LIVE |
| silver.stg_sentiment | 112 | 20:19 | ~45m | ✅ recent (low-freq) |
| gold.drift_multi_scale | 117 | 21:03:49 | ~30s | ✅ LIVE |
| gold.fct_ohlcv_features | 513,569 | **15:26** | ~5.6h | ⚠️ STALE |
| gold.fct_training_data | 513,569 | **15:26** | ~5.6h | ⚠️ STALE |
| gold.crypto_predictions | 0 | — | — | ❌ empty (batch-log table; online serve path is Feast→valkey→ml-bridge, separate) |

**Streaming half (ingest→Flink→silver→drift) is fully LIVE + current.** The **batch→gold**
path is stuck at 15:26 — the 6-hourly lakehouse DAG (#511: `merge_lakefs_branch` 400 +
`trino_quality_check`) is failing AND its post-restart re-run is stuck PodInitializing on the
recovering disk. Gold will not self-refresh until (a) disk calms so KPO children init fast and
(b) the #511 logic is fixed. Trino is consequently a DEFERRED scale-to-zero target (its
consumer is broken — would entangle).

## Scale-to-zero (#550) — newest user directive, SOURCE landed + render-validated
KEDA **cron** scale-to-zero (idleReplicaCount:0) for the two genuinely-idle pure UIs with no
broken consumers, at platform source (domain-agnostic):
- `platform/components/data-processing/superset/scaledobject.yaml` (+ removed superset/hpa.yaml,
  rewired kustomization)
- `platform/components/data-governance/datahub/scaledobject.yaml` (datahub-frontend; removed
  the frontend HPA block from datahub/hpa.yaml, kept gms+actions; rewired kustomization)
- Window: WIB 06:00–23:00 → 1 replica; overnight → 0. `kubectl kustomize` both components
  renders clean: `SO superset` / `SO datahub-frontend` present, conflicting HPAs gone, no
  orphan/dup autoscaler (KEDA refuses to reconcile beside a plain HPA — verified absent).
- **Why cron not HTTP-demand**: HTTP scale-from-zero needs the always-on http-add-on
  interceptor (permanent idle cost on one node); Prometheus-on-RPS can't wake from zero
  (no pod → no request metric). Kafka-LAG scale-from-zero (validator/analyzer) works because
  lag accrues with 0 consumers — that's why the data plane keeps event/request-driven
  scale-UP and only idle UIs get cron scale-to-zero.
- **NOT blanket**: stateful data-path (OpenSearch/ClickHouse/Kafka/MinIO/PG) stays up — most
  idle IO is theirs and can't go to zero; DataHub GMS/actions stay up (governance ingest);
  kafka-connect is broken (fix health first); Trino deferred (#511). Scaling pure UIs saves
  mostly RAM (62% used), modest IO — honest framing.
- **Apply path (GATED, not done live)**: platform Argo apps are auto-sync selfHeal=true
  **prune=false**. So land via: push source to Gitea → platform-data-processing +
  platform-data-governance auto-sync applies the SOs → then **manually
  `kubectl delete hpa superset -n data-processing` + `kubectl delete hpa datahub-frontend -n
  data-governance`** (prune=false won't auto-remove the now-Git-absent HPAs, but won't fight a
  manual delete). Do this only after the disk fully calms.

## Gated next steps (in order, each load-gated on iowait < ~25%)
1. Let disk settle (watch loadavg/vmstat; don't pile on).
2. Land #550 scale-to-zero (Gitea push → sync → delete 2 orphan HPAs).
3. Fix #511 batch→gold (lakehouse merge_lakefs_branch + trino_quality_check) so gold refreshes;
   re-verify gold latest advances past 15:26.
4. GC ~80 orphan `airflow-batch-features-*` Error pods (2–3d old; bulk field-selector delete is
   harness-blocked — delete by owning Job or exact name).

---

# 2026-06-29 ~04:40 UTC — SCALE-TO-ZERO LANDED LIVE + GOLD-REFRESH IN FLIGHT

## Scale-to-zero (#550) — DONE, live, in Gitea
- Disk recovered to steady-state (load 25, iowait 11–14%) → `make seed-gitea` pushed the full
  working tree (platform 967 files + use-case-crypto 274) to Gitea.
- Landed scale-to-zero cleanly via: Argo refresh (desired state drops the HPAs, adds the SOs)
  → delete the 2 plain HPAs → apply the 2 ScaledObjects (KEDA webhook only allows an SO once
  no HPA targets the workload — the ordering that matters). Adopted the datahub-frontend SO
  into Argo via `argocd.argoproj.io/tracking-id` annotation.
- **Verified live**: `superset` + `datahub-frontend` ScaledObjects **READY=True**;
  `keda-hpa-superset` + `keda-hpa-datahub-frontend` now own the deployments (plain HPAs gone);
  `platform-data-processing` + `platform-data-governance` both **Synced + Healthy**.
- Clock note: at apply time it was 11:30 WIB (daytime) → cron window active → UIs correctly at
  1 replica; they scale to **0 at 23:00 WIB** (16:00 UTC). ACTIVE=True is correct, not a bug.

## feature-cache retirement — landed + pruned
- The same push carried the feature-cache retirement; `crypto-use-case-local` (auto-sync
  prune=true) reconciled it. **Verified: zero feature-cache resources remain**, and no
  use-case resource is unhealthy from the prune (my change broke nothing).

## Fleet after the push: 24/30 Argo apps Synced+Healthy
6 remaining ⚠ are **pre-existing**, not caused by the push:
- chaos-mesh, opentelemetry-operator — cosmetic OutOfSync (ignoreDifferences drift).
- platform-data-ingestion (Degraded) — kafka-connect build down (#184 class).
- platform-model-serving (Degraded) — health-check-predictor crashloop (IO-bound self-test).
- platform-observability (Progressing) — transient roll.
- crypto-use-case-local (Degraded) — no individual resource unhealthy in Argo's view; stale.

## Batch→gold (#511): triggered a clean run
- The 14 `airflow-dbt-run-*` Error pods are **3 days old** (2026-06-26), GC'd logs — stale
  orphans, NOT active failures. Gold-staleness (15:26) is just that the lakehouse DAG hasn't
  completed since (post-restart run was disk-stuck).
- #511 source fix already present (lakeFS 400-tolerance, Trino QC non-fatal on empty gold).
- Disk now calm → **manually triggered `crypto_lakehouse`** (`manual__2026-06-29T04:37`) to
  refresh gold + prove #511. Monitoring gold.fct_training_data max(timestamp) for advance past
  15:26 (background waiter, with disk-spike abort).

## E2E VERIFIED GREEN (this session, live + current data)
| Link | Evidence (UTC 04:40) | Status |
|------|----------------------|--------|
| Ingestion (Coinbase→bronze) | bronze.crypto_ohlcv 827,999 rows, latest 04:39 (~1m) | ✅ LIVE |
| Stream (Flink features) | bronze.crypto_ohlcv_features 521,131, latest 04:37 | ✅ LIVE |
| Silver staging | silver.stg_ohlcv 515,156, latest 04:39 | ✅ LIVE |
| Batch→gold (training data) | gold.fct_training_data 514,830, latest 01:56 (6-hourly) | ✅ on schedule |
| Drift (multi-scale) | gold.drift_multi_scale 1,474, latest 04:38 | ✅ LIVE |
| Scale-to-zero (#550) | superset + datahub-frontend SOs READY, keda-hpa owns them | ✅ live + Gitea |
| GitOps | seed-gitea pushed; 24/30 Argo apps Synced+Healthy | ✅ |

## #511 VERIFIED PASS (the real claim, not the gold proxy)
Clean run `manual__2026-06-29T04:37` completed end-to-end:
`ensure_lakefs_repo` ✅ → `create_lakefs_branch` ✅ → `dbt_run` success ✅ →
**`merge_lakefs_branch` SUCCESS** ✅ → **`trino_quality_check` SUCCESS** ✅ (04:57:41).
The two tasks that were #511's actual failures now both pass → lakeFS WAP governance
(branch → dbt → merge-to-main → Trino federated QC) works end-to-end. Gold-staleness was a
SEPARATE disk-induced problem (resolved by the restart + scheduled run), confirming the advisor's
"two different problems" read.

## ML ONLINE-SERVING GAP (#531) — the remaining real E2E hole
- `ml-bridge /api/predictions/latest` → **503**: it proxies to
  `crypto-predictor-predictor/predictions/latest` + `/predict` (custom routes, prediction.py),
  but crypto-predictor is a **KServe MLServer** (`runtime: kserve-mlflowserver`,
  `inference.yaml`) that serves the **V2 protocol** `/v2/models/{name}/infer` → 404.
- Also `storageUri: s3://platform-models/sklearn/iris/` — the predictor points at the **iris
  demo model**, not a crypto model.
- Fix = focused MLOps pass (#529 trainer→MLflow run, #531 repoint predictor at a crypto model +
  align the ml-bridge↔predictor contract: either ml-bridge calls KServe `/v2/.../infer`, or a
  thin custom predictor shim serves `/predictions/latest`). NOT a quick patch — deferred to a
  focused pass, not rushed at session-end on a busy disk.

## #551 SERVING PASS — 90% done (2026-06-29 ~06:35 UTC), code complete + durable
The focused serving pass. **Proven: the crypto model LOADS + serves on KServe** (repointed iris→
crypto, ISVC went Ready, `/v2/models/crypto-predictor/infer` reached the model). All source fixed:
- `use-case-crypto/services/dashboard/ml-bridge/services/prediction.py` — rewritten to KServe **V2**
  (`POST /v2/models/crypto-predictor/infer`, request-level `content_type: np` so MLServer's
  mlflow-lightgbm flavor receives a 2D array; without it → 500 "Expected 2D array"). `/latest`
  reads the newest gold row, `/predict` orders a caller dict — both with **train/serve-parity**
  feature selection (numeric cols minus EXCLUDE∪TARGET, table order — TARGET `close` excluded).
- `use-case-crypto/manifests/base/inferenceservices/inference.yaml` — storageUri iris→crypto
  champion (`s3://mlflow/artifacts/1/models/m-826…/artifacts/`); reproducible.
- `platform/services/trainer/src/deploy_kserve.py` — promotion bridge now resolves the MLflow
  **3.x** model path via `search_logged_models(...).artifact_location` (was building the dead 2.x
  `{run_id}/artifacts/model/` layout → root cause the ISVC never went Ready under the bridge).
- Exact V2 codec confirmed empirically (mlserver 1.7.1): request `content_type: np`, single FP64
  tensor `[1,N]`.

**THE LAST BLOCKER (runtime, not code):** the deployed champion was trained 19h ago on a
**26-feature** gold schema; gold has since grown to **33** features → train/serve skew (live infer
returns "X has 33 features, expecting 26"). Correct fix = **retrain on current gold** (also
exercises training E2E). The retrain Job (`crypto-retrain-now`) hit the cluster's transient
**containerd sandbox-name reservation deadlock** (#191/#228 class) under a load spike (94) and was
deleted to stop its CreateContainer retry-storm. Retry on a calm disk.

**Remaining mechanical steps (all well-defined):**
1. Retrain on current gold (calm disk) → new 33-feature MLflow model → matches prediction.py.
2. Repoint: update inference.yaml storageUri to the new model path (or run the fixed
   deploy_kserve.py bridge) + add Argo `ignoreDifferences` on `spec.predictor.model.storageUri`.
3. Rebuild `crypto-ml-bridge` image (authorized in-terminal docker build) → redeploy.
4. Verify `/api/predictions/latest` returns a live crypto inference; fix main.py batch-score to
   reuse prediction.py's V2 helpers (writes gold.crypto_predictions).
5. seed-gitea (push prediction.py + inference.yaml + deploy_kserve.py) when load is calm.
- Note: the **online feature** path (Feast→valkey→ml-bridge `/api/features`) and the batch
  prediction-log are separate; this gap is specifically the model-inference call.

## Still open (gated / lower priority)
- GC ~80 orphan `airflow-batch-features-*` + `airflow-dbt-run-*` Error pods (2–3d old).
- kafka-connect build (data-ingestion Degraded) → unblocks iceberg/lakehouse archival.
- chaos-mesh / otel cosmetic OutOfSync (ignoreDifferences).
- platform-model-serving Degraded — old `crypto-predictor-predictor` ReplicaSet
  (5745c976c4) Init:CrashLoopBackOff ×84 alongside the Running one; clean up the stale RS.

## 2026-06-29 — #551 ONLINE SERVING E2E: RESOLVED + VERIFIED

`GET /api/predictions/latest?symbol=BTC-USD` → **HTTP 200**:
`{current_price: 59792.0, predicted_price: 62949.51, signal: BUY, confidence: 0.5281,
feature_count: 26, model_version: latest}`. Full chain live: ml-bridge (new image, V2
contract) → newest gold row (ClickHouse) → positional feature vector → KServe v2
`/v2/models/crypto-predictor/infer` → LightGBM champion → BUY/SELL/HOLD signal.

**Root cause of the model/serve skew (the real finding):** every trainer run logs
`features=33` (the input count) but **FLAML's DataTransformer drops the 7 near-constant
news/sentiment columns before fitting**, so every champion is a **26-feature**
OHLCV+TA+fear_greed model. Over the Jan→now training window: `sentiment_1h/6h/24h` and
`positive_ratio_24h` are **identically 0** (the sentiment scorer emits no scores — a real
data gap), and `news_count_1h/6h/24h` are near-constant (news ingestion is recent).
Confirmed by introspecting the served `booster_.feature_name()` = the 33 numeric minus
exactly those 7. Retraining would reproduce the same 26 (FLAML re-drops them).

**Fix (config, not retrain):** added the 7 news/sentiment columns to `EXCLUDE_COLUMNS`
in `use-case-crypto/manifests/base/configmaps/schema.yaml` (single source of truth →
`crypto-pipeline-config` → consumed by trainer/GE/drift AND ml-bridge `prediction.py`).
Serving now sends exactly the champion's 26 features, in table order. Trainer future
runs train on 26 honestly (no silent FLAML drop). Also hardened `prediction.py`:
feature selection is now by **ClickHouse column type** (mirrors trainer
`is_numeric_dtype`), full-width vector with NULL→0 (mirrors `fillna(0)`), so a NULL in the
newest row can never shrink the served vector below the trained width.

**Artifacts:** champion `m-0fbe5672a11d4f49ac957653374100e3` (run `ce365b57`, lgbm,
r2≈0.95), 26-feat; `inference.yaml` storageUri repointed (live ISVC patched +
`ignoreDifferences` keeps it; source pushed to Gitea); `crypto-ml-bridge:latest`
rebuilt+pushed (digest `364daf07…`, V2 + type-based prediction.py); schema.yaml
26-exclude pushed via `seed-gitea` → ConfigMap durable (selfHeal no longer reverts since
Gitea agrees). All verified live.

**KNOWN GAP (thesis-relevant, out of serving scope):** the model uses **no news/sentiment
features** because the sentiment pipeline produces only zeros and news_count is too sparse
over history for FLAML to retain. To restore sentiment as a model input: fix the sentiment
scorer so `sentiment_*`/`positive_ratio_24h` are populated, then remove those columns from
`EXCLUDE_COLUMNS` and retrain. The model is currently OHLCV + technical-indicators +
fear_greed_value only.

**SEPARATELY OPEN — batch scoring path (`ml-bridge/main.py::_run_batch_score`, writes
`gold.crypto_predictions` from the Airflow `scoring` task):** now reads the shared
`EXCLUDE_COLUMNS` env so its feature SET is auto-corrected to 26, BUT it still (a) POSTs
`{INFERENCE_URL}/predict` — the legacy custom-route contract — against the V2-only MLServer
predictor (which exposes only `/v2/models/{name}/infer`), so it 404s; and (b) selects
features by per-row `isinstance(v,(int,float))` (the NULL-fragility fixed in
`prediction.py::_feature_order`). Fix: refactor `_run_batch_score` to import + reuse
`prediction.py`'s `_infer` (V2 payload) and `_feature_order` (column-type selection), then
rebuild `crypto-ml-bridge`. Deferred here (needs another in-terminal docker build; load
touched 43 this session). The **online** path (`/api/predictions/latest|/predict`) is the
one #551 closed and is fully verified.

## 2026-06-29 (cont.) — PLATFORM-100% PASS (live audit + source fixes)

Live snapshot established the **core E2E is fresh + working**: Coinbase ingestion →
bronze/silver `11:33` → gold `09:09` (hourly dbt) → drift all 5 scales (minute→month)
succeeding today; online serving (#551) live (26-feat BUY). Node CPU 17% / MEM 52% — ample
headroom. The 88 "Error" pods were 3d–15d **orphans** (IO-cascade-era KPO debris), not
active failures.

**Fixed at source + verified this pass:**
- **Batch scoring** (`ml-bridge/main.py::_run_batch_score`) — was POSTing the legacy
  `/predict` route (404 on the V2-only predictor) → `gold.crypto_predictions` had **0 rows**.
  Refactored to reuse the verified online path (`prediction.py::_infer` + `_feature_order`).
  Rebuilt + pushed `crypto-ml-bridge` (`f97a255`). **Verified**: one-shot run wrote 2/2 rows
  (BTC 62949.51 / ETH 62970.63, BUY, lightgbm). Online serving re-verified intact on the new
  image (26-feat, 200).
- **iris platform smoke** — bootstrap Job had Failed because FLAML's ~90-wheel install
  stalled on the single HDD; the Failed hook Job then sat 14h Terminating with a stuck
  `argocd hook-finalizer` (delete-policy lacked `HookFailed`). Rewrote the train init to
  plain **sklearn LogisticRegression** (3 wheels), added `HookFailed` to the
  hook-delete-policy, stripped the stuck finalizer; PostSync hook re-creates it (re-seeding).
- **MinIO readiness flap** — `/minio/health/ready` exceeded the 5s probe timeout under IO,
  flapping MinIO's Service endpoint out (x379 / 6.5h) → `connection refused` cascading to all
  S3 clients. Relaxed probe to 15s timeout × 6 failures × 15s period (~90s sustained before
  eject; tolerates transient IO spikes).
- **Domain-agnostic platform** — `seed-gitea.sh` now auto-discovers `use-case-*/` siblings
  (was hardcoded to crypto); genericized comments in seed-gitea.sh + nuke.sh.
- **GC** — cleared 87 orphan KPO pods (use-case-crypto); platform-ns orphans left to age via
  CronJob history limits.

**4 parallel read-only audits — platform is structurally clean:**
filename "crypto" purity **0** violations · single-child folder chains **0** · idle=1 replica
invariant **0** violations (autoscalers on all load-bearing services) · UV-mandate **0** hard
violations (2 upstream-image exceptions only). Remaining audit items: 16 lone-file dirs
(cosmetic), `websocket-collector::DefaultTickerParser` carries crypto OHLCV field-knowledge
in platform/ (real domain-leak, but refactor risks the **live fresh ingestion** → deferred,
do with care).

**LESSON (reinforced):** running the Argo bootstrap-sync + a docker build concurrently spiked
load to 108 → MinIO `/ready` flap + `Insufficient cpu` scheduling delays + a transient
`CreateContainerError`. Heavy ops on this single HDD must be **serialized + load-gated**.

**Durability still pending (gated on calm load):** `seed-gitea` push of the MinIO-probe +
bucket-bootstrap-`HookFailed` + seed-gitea/nuke genericity edits (batch-score main.py + the
sklearn bootstrap are already in Gitea from the first push this session). iris live-seed is
converging via the PostSync hook retrying against now-Ready MinIO.
