apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - dex.yaml
  - oauth2-proxy.yaml
  - users-secret.yaml
  # - hpa.yaml  # opt-in: make install-hpa COMPONENT=<name> NS=<ns>