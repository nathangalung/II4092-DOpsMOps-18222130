apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Strimzi Kafka Operator 0.51.0 (2026) — ADR-011.
# CRDs + controller are installed via the ArgoCD Application in helm-release.yaml.
resources:
  - helm-release.yaml
