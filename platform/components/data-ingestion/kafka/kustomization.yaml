apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Strimzi KafkaNodePool + Kafka cluster (ADR-011) plus KEDA TriggerAuth
# and a reference ScaledObject for consumer-lag autoscaling.
resources:
  - kafka-cluster.yaml
  - scaledobject-template.yaml
