apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - hpa.yaml       # online serving — horizontal scale on request volume
  - vpa.yaml       # offline materialize — VPA(Off) advisory rightsize
  - externalsecret.yaml
  # Stub feature_store.yaml + render script (ADR-013). Use-case overlays
  # patch this same ConfigMap to add their feature_views/entities payload.
  - feature-repo.yaml
