apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Falco 0.43.1 (ADR-014 §Runtime-Detection) — eBPF-based runtime security
# DaemonSet + falcosidekick fan-out to Alertmanager/Loki/MinIO.
# Installs in its own `falco` namespace (PSS privileged; eBPF + hostPath).
resources:
  - helm-release.yaml
