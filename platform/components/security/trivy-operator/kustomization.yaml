apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Trivy Operator 0.30.1 (ADR-014 §Vuln-Scanning) — continuous image, config,
# IaC, and secret scanning exposed as Kubernetes CRDs + Prometheus metrics.
resources:
  - helm-release.yaml
