apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Chaos Mesh 2.8.2 (ADR-014) operator. Resilience experiments live in the
# evaluation harness experiments/chaos/ (one-shot CRs, run manually).
resources:
  - helm-release.yaml
