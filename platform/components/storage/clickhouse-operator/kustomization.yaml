apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Altinity ClickHouse Operator 0.26.3 (ADR-011).
# post-apply.sh clears stale CHI/CHK CRs left over from partial nuke (operator
# panics in deleteCHK nil-deref path on resurrected CRs with deletionTimestamp).
resources:
  - helm-release.yaml
