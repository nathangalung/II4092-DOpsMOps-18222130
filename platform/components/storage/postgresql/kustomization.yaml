apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# PostgreSQL via CloudNativePG.  Legacy `deployment.yaml` is replaced by the
# CNPG Cluster CR in `cluster.yaml` (ADR-011).
resources:
  - cluster.yaml
  - queries-configmap.yaml
