apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Grafana Pyroscope 2.0.1 (ADR-014) — continuous profiling (LGTM++ stack).
# See helm-release.yaml for ingester topology + eBPF DaemonSet config.
resources:
  - helm-release.yaml
