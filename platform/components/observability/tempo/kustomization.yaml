apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Grafana Tempo 2.10.4 (ADR-010) — OTLP trace backend on MinIO S3.
# See helm-release.yaml for receiver/exporter topology.
resources:
  - helm-release.yaml
