apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# OpenTelemetry Operator + gateway Collector + agent DaemonSet + auto-instrumentation (ADR-010).
resources:
  - helm-release.yaml
  - collectors.yaml
