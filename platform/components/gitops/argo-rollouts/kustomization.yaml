apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Argo Rollouts 1.9.0 (ADR-016) — progressive delivery (canary / blue-green).
# Istio traffic routing + Prometheus-gated AnalysisTemplate. See
# helm-release.yaml for the reference AnalysisTemplate shape.
resources:
  - helm-release.yaml
