# Airflow providers for MLOps platform orchestration.
# This file is a human-readable mirror of pyproject.toml's `dependencies =`
# list — uv sync reads pyproject.toml + uv.lock, not this file.  Keep the
# two in lockstep.
apache-airflow-providers-cncf-kubernetes>=4.3.0
apache-airflow-providers-postgres>=6.0.0
apache-airflow-providers-amazon>=9.6.0
apache-airflow-providers-fab>=3.6.1
psycopg2-binary>=2.9.10
asyncpg>=0.30.0
