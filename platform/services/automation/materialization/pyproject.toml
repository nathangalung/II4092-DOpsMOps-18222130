[project]
name = "materialization"
version = "1.0.0"
description = "Feast feature materialization job"
requires-python = ">=3.14,<3.15"
dependencies = [
    # Feast with ClickHouse offline store + Valkey online store (Redis-RESP wire protocol).
    # Feast extra is named `redis`; Valkey 9 is drop-in compatible (BSD-3-Clause vs Redis 8 AGPL).
    "feast[clickhouse,redis]>=0.61.0",
    # ClickHouse driver (feast[clickhouse] pulls it transitively; explicit floor for security).
    "clickhouse-connect>=0.14.0",
    # boto3 for Feast's S3 file registry (registry_type: file, path: s3://feast/...).
    # feast[clickhouse,redis] does NOT pull an S3 client (verified absent from
    # uv.lock); S3RegistryStore reads/writes the registry proto via boto3.
    "boto3>=1.34",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]

[dependency-groups]
dev = ["pytest>=8.3.4", "pytest-cov>=6.0.0", "pytest-mock>=3.14.0", "ruff>=0.8.4"]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "N", "UP", "ANN", "B", "C4", "SIM"]
ignore = ["ANN401"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --cov=. --cov-report=term-missing"
