# =============================================================================
# Kafka SASL_SSL wiring for use-case-crypto workloads (cycle-3 §3.1)
# =============================================================================
# Adds the `crypto-app-consumer` Secret as envFrom (KAFKA_SASL_USERNAME /
# KAFKA_SASL_PASSWORD / sasl.jaas.config / ca.crt) and mounts the broker CA
# at `/etc/kafka/ca/ca.crt` (matches `KAFKA_SSL_CA_LOCATION` in topics.yaml)
# on every Kafka-touching workload in this use case:
#   - validator             (Rust, librdkafka)
#   - analyzer              (Python, confluent-kafka)
#   - rest-collector        (Python producer)
#   - websocket-collector   (Python producer)
#
# Strategic merge replaces the `envFrom` list, so each stanza re-declares the
# platform's `pipeline-config` + `pipeline-secrets` refs alongside the new
# `crypto-app-consumer` secretRef.  Volume + volumeMount append cleanly via
# the `name` mergeKey.
#
# The Flink stream-processor is NOT patched here — it carries the identical
# credential surface (envFrom crypto-app-consumer + the /etc/kafka/ca mount)
# inline in its FlinkDeployment podTemplate (flink/stream-processor.yaml),
# because a strategic-merge Deployment patch cannot target a FlinkDeployment CR.
# dashboard-backend / gateway / ml-bridge are not Kafka clients.
# =============================================================================
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: validator
spec:
  template:
    spec:
      containers:
        - name: validator
          envFrom:
            - configMapRef:
                name: pipeline-config
            - secretRef:
                name: pipeline-secrets
            - secretRef:
                name: crypto-app-consumer
          volumeMounts:
            - name: kafka-ca
              mountPath: /etc/kafka/ca
              readOnly: true
      volumes:
        - name: kafka-ca
          secret:
            secretName: crypto-app-consumer
            items:
              - key: ca.crt
                path: ca.crt
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: analyzer
spec:
  template:
    spec:
      containers:
        - name: analyzer
          envFrom:
            - configMapRef:
                name: pipeline-config
            - secretRef:
                name: pipeline-secrets
            - secretRef:
                name: crypto-app-consumer
          volumeMounts:
            - name: kafka-ca
              mountPath: /etc/kafka/ca
              readOnly: true
      volumes:
        - name: kafka-ca
          secret:
            secretName: crypto-app-consumer
            items:
              - key: ca.crt
                path: ca.crt
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rest-collector
spec:
  template:
    spec:
      containers:
        - name: rest-collector
          envFrom:
            - configMapRef:
                name: pipeline-config
            - secretRef:
                name: pipeline-secrets
            - secretRef:
                name: crypto-app-consumer
          volumeMounts:
            - name: kafka-ca
              mountPath: /etc/kafka/ca
              readOnly: true
      volumes:
        - name: kafka-ca
          secret:
            secretName: crypto-app-consumer
            items:
              - key: ca.crt
                path: ca.crt
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: websocket-collector
spec:
  template:
    spec:
      containers:
        - name: websocket-collector
          envFrom:
            - configMapRef:
                name: pipeline-config
            - secretRef:
                name: pipeline-secrets
            - secretRef:
                name: crypto-app-consumer
          volumeMounts:
            - name: kafka-ca
              mountPath: /etc/kafka/ca
              readOnly: true
      volumes:
        - name: kafka-ca
          secret:
            secretName: crypto-app-consumer
            items:
              - key: ca.crt
                path: ca.crt
