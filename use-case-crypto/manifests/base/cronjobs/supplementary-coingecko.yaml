# CoinGecko market data — free Demo plan (30 req/min, needs free API key)
# Runs every hour, fetches market overview and trending data
# Emits run outcome to platform Pushgateway via push-helper ConfigMap.
apiVersion: batch/v1
kind: CronJob
metadata:
  name: supplementary-coingecko
  labels:
    app: supplementary-coingecko
    component: ingestion
    mlops-platform.io/use-case: crypto
spec:
  schedule: "15 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    # Kueue admission: Job-level label routes to batch-queue → cluster cohort.
    metadata:
      labels:
        kueue.x-k8s.io/queue-name: crypto-batch-queue
    spec:
      backoffLimit: 2
      # 600→1800: see supplementary.yaml header. Paired with IfNotPresent
      # below so only the first cold-cache fire absorbs the long pull.
      activeDeadlineSeconds: 1800
      template:
        metadata:
          labels:
            app: supplementary-coingecko
        spec:
          restartPolicy: OnFailure
          # PodSecurity "restricted" baseline, paired with Dockerfile
          # USER 1000:1000.
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            seccompProfile:
              type: RuntimeDefault
          volumes:
            - name: push-helper
              configMap:
                name: push-helper
                defaultMode: 0555
            # rest-collector binary opens /etc/rest-collector/config.yaml at
            # startup; without the mount the Job crashes on `open ... no such
            # file` before any HTTP fetch fires (mirrors the long-running
            # rest-collector Deployment patch).
            - name: rest-collector-config
              configMap:
                name: crypto-rest-collector-config
            # Kafka SASL_SSL CA — required by librdkafka producer in rest-collector
            # (KAFKA_SSL_CA_LOCATION=/etc/kafka/ca/ca.crt). patches/kafka-sasl.yaml
            # only patches Deployments; CronJobs embed the mount directly.
            - name: kafka-ca
              secret:
                secretName: crypto-app-consumer
                items:
                  - key: ca.crt
                    path: ca.crt
          containers:
            - name: rest-collector
              image: rest-collector:latest
              # IfNotPresent — see #167 / #402.
              imagePullPolicy: IfNotPresent
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: false
                capabilities:
                  drop: ["ALL"]
              # Wrap the Go binary in a shell that captures rc + duration
              # and emits to Pushgateway on exit (success or failure).
              command: ["/bin/sh", "-c"]
              args:
                - |
                  set -u
                  . /scripts/push_job_metrics.sh
                  run_and_push /app/rest-collector --once
              envFrom:
                - configMapRef:
                    name: pipeline-config
                - secretRef:
                    name: pipeline-secrets
                - secretRef:
                    name: crypto-app-consumer
              env:
                - name: HISTORY_BACKFILL_ON_START
                  value: "false"
                - name: POLLING_ENABLED
                  value: "false"
                - name: SUPPLEMENTARY_SOURCE_NAME
                  value: "coingecko"
                - name: SUPPLEMENTARY_SOURCE_URL
                  value: "https://api.coingecko.com/api/v3/coins/markets?vs_currency=usd&ids=bitcoin,ethereum&order=market_cap_desc&per_page=10&page=1&sparkline=false"
                - name: SUPPLEMENTARY_SOURCE_ENABLED
                  value: "true"
                - name: SUPPLEMENTARY_SOURCE_POLL_INTERVAL
                  value: "0s"
                - name: SUPPLEMENTARY_SOURCE_API_KEY
                  valueFrom:
                    secretKeyRef:
                      name: pipeline-secrets
                      key: COINGECKO_API_KEY
                      optional: true
                - name: SUPPLEMENTARY_SOURCE_API_KEY_HEADER
                  value: "x-cg-demo-api-key"
                - name: SUPPLEMENTARY_BACKFILL_ENABLED
                  value: "true"
                - name: SUPPLEMENTARY_BACKFILL_START_DATE
                  value: "2026-03-01T00:00:00Z"
                - name: COINGECKO_BACKFILL_COINS
                  value: "bitcoin,ethereum"
                - name: KAFKA_SUPPLEMENTARY_TOPIC
                  value: "crypto.supplementary"
                - name: JOB_NAME
                  value: "supplementary_coingecko"
                - name: USE_CASE
                  value: "crypto"
              volumeMounts:
                - name: push-helper
                  mountPath: /scripts
                  readOnly: true
                - name: rest-collector-config
                  mountPath: /etc/rest-collector
                  readOnly: true
                - name: kafka-ca
                  mountPath: /etc/kafka/ca
                  readOnly: true
              resources:
                requests:
                  cpu: "50m"
                  memory: "64Mi"
                limits:
                  cpu: "200m"
                  memory: "128Mi"
